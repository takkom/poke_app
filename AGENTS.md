# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.
Design System: Use established theme colors and tokens. Create or re-use modular, reusable components where appropriate. Do not hardcode raw hex values, spacing, or styles if theme variables exist.

Localization & Themes: Before marking a task complete, explicitly verify and test for:

Light and dark theme consistency and contrast.

Internationalization (i18n)—ensure all new user-facing strings use localization keys, with no hardcoded UI text.

Clarification Protocol: If a requirement is ambiguous, conflicts with existing architecture, or compromises UX/usability/performance, stop and ask before writing code or implementing. Propose your approach and wait for confirmation.
